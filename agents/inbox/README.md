# Inbox

Los agentes dejan acá su reporte cuando terminan una tarea.

Formato de archivo: `<agent_id>-<YYYYMMDD-HHMMSS>.md`
Ver `agents/PROTOCOLO.md` para el schema del reporte.

Hermes (coordinador) procesa los reportes cada 10 min y los archiva en `archive/<año>/<mes>/`.
